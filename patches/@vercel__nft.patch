diff --git a/out/utils/ast-helpers.js b/out/utils/ast-helpers.js
index 72479bdd04d4f27b4cac53d87ce0c588bb1b8df8..82e6946930060cbb65b1b9b047ef9c0a566c0459 100644
--- a/out/utils/ast-helpers.js
+++ b/out/utils/ast-helpers.js
@@ -25,7 +25,10 @@ function isIdentifierRead(node, parent) {
             return parent.id !== node;
         // disregard the `bar` in `export { foo as bar }`
         case 'ExportSpecifier':
-            return false;
+            if (node.name === 'NODE') {
+                console.log(node.name, parent.exported.name);
+            }
+            return node.name === parent.exported.name;
         // disregard the `bar` in `function (bar) {}`
         case 'FunctionExpression':
         case 'FunctionDeclaration':
